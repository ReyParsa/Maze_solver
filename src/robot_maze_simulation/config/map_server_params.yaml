map_server:
  ros__parameters:
    # If left empty, no map is loaded during on_configure; we override this from the launch file.
    yaml_filename: ""
    # Additional parameters can be added here if needed (e.g., topic names, QoS).
